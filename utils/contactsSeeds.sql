DROP DATABASE IF EXISTS aclockDB;
CREATE DATABASE aclockDB;

-- USE aclockDB;


-- CREATE TABLE contacts (
--     id INT AUTO_INCREMENT NOT NULL,
--     contact_name VARCHAR (100) NOT NULL,
--     email VARCHAR (45),
--     phone_num VARCHAR(20   ) NOT NULL,
--     contact_message VARCHAR(250) NOT NULL,
--     PRIMARY KEY (id)
-- );

-- INSERT INTO contacts (contact_name, email, phone_num, contact_message)

-- VALUES("Taylor Owens-Kees", "taylorowenskees@outlook.com", "1-510-878-8871", "Hi, I lost my spare car key and need a new one. I'll also need it reprogrammed to my car")

-- SELECT * FROM contacts